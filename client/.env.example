# Copy this file to client/.env.local for local development against the
# deployed Render backend instead of the local dev server proxy.
# In production this is set as a Netlify build environment variable
# (Site settings -> Environment variables), not committed here.
VITE_API_BASE_URL=https://rameshwaram-qsr-server.onrender.com/api
